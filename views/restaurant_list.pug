extends layout 

block content 
  h1= title 

  ul 
    each restaurant in restaurant_list
      li 
        a(href=restaurant.url) #{restaurant.name}
    
    else
      li There are no restaurants 
      